import React from "react";

const MessengerKidsIcon = ({ iconPath }) => {
  return (
    <div>
      <img
        draggable="false"
        height="36"
        width="36"
        alt="MessengerKidsIcon"
        referrerPolicy="origin-when-cross-origin"
        src={iconPath}
      ></img>
    </div>
  );
};

export default MessengerKidsIcon;
